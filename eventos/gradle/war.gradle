apply plugin: "war"

bootWar {
    mainClassName = "com.dai.eventos.EventosApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
